version: "3.7"
services:
  server:
    container_name: iobroker
    image: iobroker/iobroker:latest
    volumes:
      - ${APP_DATA_DIR}/data/work:/opt/iobroker
    restart: always
        networks:
      # Specify the adguard-home-network
      - iobroker-network
# Networks used by the application
networks:
  # Define the network for AdGuard Home
  iobroker-network:
    driver: bridge
