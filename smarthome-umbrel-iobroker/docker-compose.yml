version: "3.7"
services:
  app:
    image: ghcr.io/buanet/iobroker:latest
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/config:/config
      - /etc/localtime:/etc/localtime:ro
      - /run/dbus:/run/dbus:ro
    network_mode: host
