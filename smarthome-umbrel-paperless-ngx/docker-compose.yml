# Configuration for paperless-ngx setup
# Name of the smarthome-paperless-ngx application
version: "3.7"
# Service definitions for the smarthome-paperless-ngx application
services:
  app_proxy:
    environment:
      APP_HOST: smarthome-umbrel-paperless-ngx_smarthome-paperless-ngx_1
      APP_PORT: "8000"
  # Service name: smarthome-paperless-ngx
  # The `smarthome-paperless-ngx` service definition
  smarthome-paperless-ngx:
    # Image to be used for the container
    image: ghcr.io/paperless-ngx/paperless-ngx:2.20.3
    # Container restart policy
    restart: unless-stopped
    # environment variables
    environment:
      PAPERLESS_REDIS: redis://smarthome-paperless-ngx-broker:6379
      PAPERLESS_DBHOST: smarthome-paperless-ngx-db
      PAPERLESS_DBUSER: smarthome
      PAPERLESS_DBPASS: c6e74adb-3fce-4318-b657-4bdc0858fcac
      PAPERLESS_ADMIN_USER: smarthome
      PAPERLESS_ADMIN_PASSWORD: c22e92a6-2a3d-4edf-a98e-4044834daea6
      PAPERLESS_TIKA_ENABLED: "true"
      PAPERLESS_TIKA_GOTENBERG_ENDPOINT: http://smarthome-paperless-ngx-gotenberg:3000
      PAPERLESS_TIKA_ENDPOINT: http://smarthome-paperless-ngx-tika:9998
      PAPERLESS_URL: http://[YOUR_IP]:8000
      COMPOSE_PROJECT_NAME: smarthome-paperless-ngx
      PAPERLESS_CSRF_TRUSTED_ORIGINS: http://[YOUR_IP]:8000
    # Volumes to be mounted to the container
    volumes:
      - ${APP_DATA_DIR}/paperless/data:/usr/src/paperless/data
      - ${APP_DATA_DIR}/paperless/media:/usr/src/paperless/media
      - ${APP_DATA_DIR}/paperless/export:/usr/src/paperless/export
      - ${APP_DATA_DIR}/paperless/consume:/usr/src/paperless/consume
      - ${APP_DATA_DIR}/paperless/tmp:/tmp
    depends_on:
      - smarthome-paperless-ngx-db
      - smarthome-paperless-ngx-broker
    networks:
      - smarthome-paperless-ngx-network
  smarthome-paperless-ngx-broker:
    image: docker.io/library/redis:8.4.0
    restart: unless-stopped
    volumes:
      - ${APP_DATA_DIR}/redis:/data
    networks:
      - smarthome-paperless-ngx-network
  # The PostgreSQL database service is used to store the data for the paperless-ngx service.
  smarthome-paperless-ngx-db:
    # Image to be used for the container
    image: library/postgres:16
    # Container restart policy
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      - ${APP_DATA_DIR}/postgres:/var/lib/postgresql/data
    # Environment variables
    environment:
      # PostgreSQL database name
      POSTGRES_DB: paperless
      # PostgreSQL database user
      POSTGRES_USER: smarthome
      # PostgreSQL database password
      POSTGRES_PASSWORD: c6e74adb-3fce-4318-b657-4bdc0858fcac
    # Networks to be used for the service
    networks:
      - smarthome-paperless-ngx-network
  # The gotenberg service is used for converting documents to PDFs. It is
  # configured to disable JavaScript and only allow access to the
  # /tmp directory to prevent potential security issues.
  smarthome-paperless-ngx-gotenberg:
    image: gotenberg/gotenberg:8.25
    restart: unless-stopped
    # The gotenberg chromium route is used to convert .eml files. We do not
    # want to allow external content like tracking pixels or even javascript.
    # Therefore, we disable JavaScript and only allow access to the
    # /tmp directory.
    command:
      - "gotenberg"
      - "--chromium-disable-javascript=true"
      - "--chromium-allow-list=file:///tmp/.*"
    networks:
      - smarthome-paperless-ngx-network
  # The Tika service is used to extract text from files. It is based on the
  # Apache Tika project and is configured to use the minimal image.
  smarthome-paperless-ngx-tika:
    image: apache/tika:3.2.3.0
    restart: unless-stopped
    # Volumes to be mounted to the container
    volumes:
      - ${APP_DATA_DIR}/tika/tmp:/tmp
    # The Tika service is connected to the smarthome-paperless-ngx-network
    # network.
    networks:
      - smarthome-paperless-ngx-network
# Define a network for the paperless-ngx services to communicate with
# each other. This network is a bridge network, which means that it is
# not isolated from the host's network stack.
networks:
  smarthome-paperless-ngx-network:
    driver: bridge
